[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "bilinote-workflow"
version = "0.1.0"
description = "BiliNote-based video summary workflow"
requires-python = ">=3.12"
